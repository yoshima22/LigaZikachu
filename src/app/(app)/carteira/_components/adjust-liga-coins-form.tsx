"use client";import{useState,useTransition}from"react";import{toast}from"sonner";import{Button}from"@/components/ui/button";import{adjustLigaCoins}from"../actions";
export function AdjustLigaCoinsForm({players}:{players:{id:string;displayName:string}[]}){const[pending,start]=useTransition();const[form,setForm]=useState({playerId:"",amount:0,description:""});return <div className="grid gap-3 sm:grid-cols-[1fr_120px_1fr_auto]"><label className="space-y-1 text-xs text-slate-400"><span>Jogador</span><select value={form.playerId} onChange={e=>setForm({...form,playerId:e.target.value})} className="w-full rounded-lg border border-border bg-slate-950 px-3 py-2 text-sm"><option value="">Selecione</option>{players.map(p=><option key={p.id} value={p.id}>{p.displayName}</option>)}</select></label><label className="space-y-1 text-xs text-slate-400"><span>LC (+ ou -)</span><input type="number" value={form.amount} onChange={e=>setForm({...form,amount:Number(e.target.value)})} className="w-full rounded-lg border border-border bg-slate-950 px-3 py-2 text-sm"/></label><label className="space-y-1 text-xs text-slate-400"><span>Motivo</span><input value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="w-full rounded-lg border border-border bg-slate-950 px-3 py-2 text-sm"/></label><div className="flex items-end"><Button disabled={pending} onClick={()=>start(async()=>{const r=await adjustLigaCoins(form.playerId,form.amount,form.description);if(r.error)toast.error(r.error);else{toast.success("LigaCoins ajustadas.");setForm({playerId:"",amount:0,description:""})}})} className="bg-cyan-300 text-slate-950">Aplicar LC</Button></div></div>}
